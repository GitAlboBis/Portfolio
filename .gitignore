# dependencies
/node_modules
/.pnp
.pnp.*

# package manager: bun is canonical (bun.lock). Keep stray npm/yarn lockfiles out.
/package-lock.json
/yarn.lock

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem
desktop.ini

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# large working video assets (hero clip source — add later via Git LFS, not raw git)
/public/video/

# docs-driven-build loop: run-time screenshot artifacts (state ledger IS committed)
/loops/docs-driven-build/artifacts/

# design-system research scratch (captured in DESIGN-SYSTEM.md)
/.wf-out/

# reference repo clones studied for the water sim (Splash / WaterBall) — not ours
/.analysis-clones/
